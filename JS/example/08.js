// 題目 2: 自定義 Promise 延遲函數
// 題目： 寫一個 delay 函數，它返回一個延遲指定時間的 Promise。你可以使用這個 delay 函數來模擬處理某些操作（例如請求、計算等）。
// 這個 delay 函數應該接受一個時間（以毫秒為單位）作為參數，並在指定時間後解析。


function delay(ms) {
    // 請在這裡實現代碼
}


delay(1000)
    .then(() => {
        console.log("1 second has passed");
    });

delay(2000)
    .then(() => {
        console.log("2 seconds have passed");
    });
